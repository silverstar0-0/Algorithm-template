#include <bits/stdc++.h>
using namespace std;
const int maxn=100005;
int n,m,point[maxn],nextp[maxn],to[maxn];
void create(void){
	int temp;
	cin>>n>>m;
	for(int i=1;i<=m;i++){
		cin>>temp;
		nextp[i]=point[temp];
		point[temp]=i;
		cin>>to[i];
	}
}
//n为节点数量，m为边数量
//本程序适用于不带权有向图的建图 
